/*
 * edcan_config.h
 *
 * Файл конфигурации библиотеки EDCAN.
 * Скопируйте этот файл в ваш проект и переименуйте в edcan_config.h
 * Отредактируйте настройки в соответствии с вашим проектом.
 */

#ifndef EDCAN_CONFIG_H
#define EDCAN_CONFIG_H

/* ============================================================================
 * ОСНОВНЫЕ НАСТРОЙКИ УСТРОЙСТВА
 * ============================================================================
 */

/* Device ID - тип устройства (используется в регистре 0x01) */
#define DEVICE_ID 0x40

/* Версия прошивки (используется в регистрах 0x02, 0x03, 0x04) */
#define FWVER_MAJOR 1
#define FWVER_MINOR 0
#define FWVER_PATCH 0

/* Для обратной совместимости (если используется старый код) */
#define FWVER 1

/* ============================================================================
 * НАСТРОЙКА CAN ПЕРИФЕРИИ
 * ============================================================================
 * Выберите используемый CAN контроллер в зависимости от вашего микроконтроллера
 * Раскомментируйте соответствующую секцию
 */

/* Вариант 1: STM32 с одним CAN контроллером (например, STM32F0) */
#define ED_CANx
extern CAN_HandleTypeDef hcan;
#define ED_CAN_INSTANCE hcan

/* Вариант 2: STM32 с двумя CAN контроллерами - используем CAN1 */
//#define ED_CAN1
//extern CAN_HandleTypeDef hcan1;
//#define ED_CAN_INSTANCE hcan1

/* Вариант 3: STM32 с двумя CAN контроллерами - используем CAN2 */
//#define ED_CAN2
//extern CAN_HandleTypeDef hcan2;
//#define ED_CAN_INSTANCE hcan2

/* ============================================================================
 * НАСТРОЙКА БУФЕРОВ
 * ============================================================================
 */

/* Размер буферов приема и передачи (в элементах)
 * Можно уменьшить для экономии памяти
 * Рекомендуемые значения: 64, 128, 256
 */
#define BUFFER_SIZE 128

/* ============================================================================
 * НАСТРОЙКА РЕГИСТРА ПЕРЕЗАГРУЗКИ
 * ============================================================================
 */

/* Адрес регистра для удаленной перезагрузки устройства
 * По умолчанию: 0x26
 * Для перезагрузки нужно записать значение 0x66 в этот регистр
 */
//#define EDCAN_RESET_REG 0x27

/* ============================================================================
 * НАСТРОЙКА BOOTLOADER (OpenBLT)
 * ============================================================================
 * Раскомментируйте для поддержки входа в bootloader через EDCAN
 */

/* Регистр для входа в bootloader
 * Запись в этот регистр значения [FF 00] активирует bootloader
 */
//#define EDCAN_BOOTLOADER_BACKDOOR_REG 0x80

/* Адрес в RAM для магического значения bootloader
 * ВАЖНО: Выберите адрес, который НЕ очищается при старте до проверки bootloader
 * Обычно используется конец RAM области
 */
//#define EDCAN_BOOTLOADER_MAGIC_ADDR  0x2000FFFC

/* Магическое значение для активации bootloader */
//#define EDCAN_BOOTLOADER_MAGIC_VALUE 0xEDB00780

#endif // EDCAN_CONFIG_H
